---
'draw-axis-p5js': minor
---

Reescritura en TypeScript con tipos publicados (`dist/*.d.ts`), tests con `node --test` sobre el build real y pipeline de release verificado. Reemplaza el build stale de 1.0.0 publicado en npm.
